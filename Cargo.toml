[package]
name = "searchengine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
fuzzy-matcher = "0.3"
regex = "1.11"
once_cell = "1.20"
log = "0.4"

# LibAnything — низкоуровневое ядро (нужно только для линковки, FFI)
libanything = { path = "../LibAnything" }
